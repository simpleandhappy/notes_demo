import React from 'react'

const Header = () => {
    //react components can only have one parent div, or fragment (<> </>)
    return(
        <div>
            <h1> Notes App Header </h1>
        </div>
    )
}

export default Header
